{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
        {{ form_row(form.username, {attr:{'placeholder':'nom de l\'utilisateur'}}) }}
        {{ form_row(form.email, {attr:{'placeholder':'nom de l\'utilisateur'}}) }}
        {{ form_row(form.password, {attr:{'placeholder':'nom de l\'utilisateur'}}) }}
        {{ form_end(form) }}
        <button type="submit" class ="btn btn-success">Inscription</button>
    </div>
{% endblock %}
