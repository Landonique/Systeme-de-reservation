{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary"><i class="fa fa-check"></i> {{ button_label|default('Enregistrer') }}</button>
    <a href="{{ path('user_index') }}" class="btn btn-light">
        Annuler
    </a>
{{ form_end(form) }}
