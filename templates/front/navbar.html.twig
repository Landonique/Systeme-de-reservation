<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">CoVOITURAGE</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse ml-3" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            {% if 'ROLE_CHAUFFEUR' in app.user.roles %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('page') }}">
                        <i class="far fa-list-alt"></i>
                        Mes réservations <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link font-weight-bold text-warning" href="{{ path('driver_cars', {id: app.user.id}) }}">
                        <i class="fas fa-car"></i>
                        Mes Voitures <span class="sr-only">(current)</span>
                    </a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('page') }}">
                        <i class="fa fa-home"></i>
                        Accueil <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link font-weight-bold text-warning" href="{{ path('cars') }}">
                        <i class="fas fa-car"></i>
                        Nos Voitures <span class="sr-only">(current)</span>
                    </a>
                </li>
            {% endif %}
        </ul>
        <div class="form-inline">
            <span class="text-light h6 mr-5">
                <i class="fa fa-user"></i> {{ app.user.lastname }}
            </span>
            <a href="{{ path('app_logout') }}" class="h6 text-white text-decoration-none">
                <i class="fas fa-sign-out-alt"></i> Déconnexion
            </a>
        </div>
    </div>
</nav>
{% if 'ROLE_CHAUFFEUR' not in app.user.roles %}
<div class="jumbotron jumbotron-fluid mb-1">
    <div class="container">
        <h1 class="display-4">C'est facile :)</h1>
        <p class="lead">Commander un transfert à travers notre site internet est plus economique qu'appeler un taxi.</p>
    </div>
</div>
{% endif %}
