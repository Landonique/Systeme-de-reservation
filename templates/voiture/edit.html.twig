{% extends 'base.html.twig' %}

{% block title %}Edit Voiture{% endblock %}

{% block body %}
<div class='container mt-4'>
    <h1>Edit Voiture</h1>
     
        {{ include('voiture/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex justify-content-center"> 
        <a href="{{ path('voiture_index') }}" class="btn btn-primary stretched-link mr-4">Back to list</a>

        {{ include('voiture/_delete_form.html.twig') }}
    </div>
</div>    
{% endblock %}
